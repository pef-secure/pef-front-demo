---
params:
  ip: $ip
  auth: 
    base: $auth
    optional: true
model: Author::logout
result:
  DEFAULT:
    redirect:
        - TT headers('referer') 
        - /
    unset-cookie: auth
expected:
  - result: OK
  - result: $DEFAULT
    