---
params:
  ip: $ip
  auth: 
    base: $auth
    optional: true
model: Author::logout
result:
  DEFAULT:
    redirect:
        - TT defaults.path_info 
        - /
    unset-cookie: auth
expected:
  - result: OK
  - result: $DEFAULT
    